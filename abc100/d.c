#include <stdio.h>

int main (void)
{
    int n, m;
    long long x[1000], y[1000], z[1000];
    int i;

    scanf("%d %d", &n, &m);
    for (i = 0; i < n; i++) {
        scanf("%lld %lld %lld", &x[i], &y[i], &z[i]);
    }

}
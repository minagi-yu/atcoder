#include <stdio.h>

int main (void)
{
    int d, g, i, mondaisu = 0, ans, tmp;
    int p[10], c[10], sum[10];

    scanf("%d %d", &d, &g);
    for (i = 0; i < d; i++) {
        scanf("%d %d", &p[i], &c[i]);
        sum[i] = (i + 1) * 100 + c[i];
    }

    

}
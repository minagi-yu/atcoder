#include <stdio.h>

int nain (void)
{
    int N, K, x[100000] = {0};
    int i, min;

    scanf("%d %d", &N, &K);
    for (i = 0; i < N; i++) {
        scanf("%d", &x[i]);
    }

    for (i = 0; i < N - K + 1; i++) {
        
    }

}